export class va {
    //static HOST = "muliniavento87.ignorelist.com";
    static HOST = "localhost";
    static PORT = "8001";
    static HOST_AND_PORT = `${va.HOST}:${va.PORT}`;
    // eslint-disable-next-line
    static URL = `http://${va.HOST_AND_PORT}`;
}